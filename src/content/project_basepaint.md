An open-source archival pipeline for the generative art community `basepaint.xyz`. Architected an asynchronous **Python** orchestrator (`asyncio`, `tenacity`) using `pydantic` schemas to enforce deterministic, structured **LLM** outputs from rate-limited vision models, generating automated, high-quality PDF metadata archives.
